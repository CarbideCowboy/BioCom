<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.hoker.biocom">

    <application
        android:allowBackup="true"
        android:icon="@drawable/app_icon"
        android:label="@string/app_name"
        android:roundIcon="@drawable/app_icon"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".pages.EraseTag"></activity>
        <activity android:name=".pages.NdefEditTextPayload" />
        <activity android:name=".pages.NdefWriteText" />
        <activity android:name=".pages.NdefDecrypt" />
        <activity android:name=".pages.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            <intent-filter>
                <action android:name="android.nfc.action.NDEF_DISCOVERED" />

                <category android:name="android.intent.category.DEFAULT" />

                <data android:mimeType="text/plain" />
            </intent-filter>
        </activity>
        <activity android:name=".pages.NdefReadText" />
        <activity android:name=".pages.NdefScanTextPrompt" />
    </application>

    <uses-permission android:name="android.permission.NFC" />

</manifest>